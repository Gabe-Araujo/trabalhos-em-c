#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cont = 10;
    int fim = 0; //perguntar ao profesor o porque precisa da variavel fim e nao colocar apenas o numeral

  do //vai fazer isso// executar pelo menos uma vez
    {
        printf("\n%d",cont);
        cont --;
    }
  while(cont > fim ); //enquanto cont for maior que o fim



}
